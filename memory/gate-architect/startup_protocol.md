# gate-architect Startup Protocol

1. Read `memory/gate-architect/notes.md`.
2. Read `agents/profiles/coordinated/gate-architect/role_prompt.md`.
3. Confirm you are `gate-architect`.
4. Wait for a message in `dispatch/gate-architect/inbox/`.
5. After pickup, move the message to `dispatch/gate-architect/done/`.
6. Read `.orchestrator/diffs/{task_id}.diff` before reviewing.
