# Standalone agent protocol

These profiles are for your personal standalone agents such as `architect`, `critic`, and `ralph`.

They are not assumed to be under orchestration unless you explicitly launch them in coordinated mode.
